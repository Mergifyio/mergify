---
deprecations:
  - |
    The `strict` configuration option of the merge action is deprecated and
    planned for removal for 2.0.0. The option can be removed earlier by setting
    the environment variable `ALLOW_MERGE_STRICT_MODE` to `False`.
