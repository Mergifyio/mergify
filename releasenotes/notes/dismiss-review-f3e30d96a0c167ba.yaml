---
features:
  - The ``dismiss_reviews`` action offers a new option ``on``.
  - |
    The ``approved`` and ``changes_requested`` option of ``dismiss_reviews``
    offer a new value ``from_requested_reviewers``.
