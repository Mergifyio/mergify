init_config:

instances:
 - host: <CACHE HOST>
   port: <CACHE PORT>
   password: <CACHE PASSWORD>
   tags:
     - role:cache

 - host: <CELERY HOST>
   port: <CELERY PORT>
   password: <CELERY PASSWORD>
   keys:
     - celery
     - schedule
     - mergify
   tags:
     - role:celery
