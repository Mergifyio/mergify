init_config:

instances:
  - host: <CACHE HOST>
    port: <CACHE PORT>
    password: <CACHE PASSWORD>
    tags:
      - role:cache

  - host: <STREAM HOST>
    port: <STREAM PORT>
    password: <STREAM PASSWORD>
    keys:
      - streams
      - attempts
    tags:
      - role:streams
